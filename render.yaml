# Render.com deployment configuration
services:
  - type: web
    name: medium-search-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api_deploy:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
